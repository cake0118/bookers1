<h1>Books</h1>
<% @books.each do |book| %>

  <p>title</p>
  <span><%= book.title %></span>

  <p>body</p>
  <span><%= book.body %></span>

　<p></p>
  <span>
   <%= link_to "Show", book_path(book.id)%>
  </span>
  <span>
    <%= link_to "Edit", book_path(book.id) %>
  </span>
  <span>
    <%= link_to "Destroy", destroy_book_path(book.id), method: :delete, "date-confirm" => "Are you sure?" %>
  </span>


<% end %>
