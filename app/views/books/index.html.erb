<h1>Books</h1>
<% @books.each do |book| %>

  <p>title</p>
  <span><%= book.title %></span>

  <p>body</p>
  <span><%= book.body %></span>

  <p>
   <%= link_to "Show", books_path(@books.id)%>
  </p>
  <p>
    <%= link_to "Edit", books_path(@books.id) %>
  </p>
  <p>
    <a href="/books">Destroy</a>
  </p>


<% end %>
